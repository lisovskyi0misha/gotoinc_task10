<nav class="navbar navbar-expand-lg bg-light">
    <div class="container-fluid">
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <% if user_signed_in?%>
                    <% if current_user.admin?%>
                        <li class="nav-item">
                            <%= link_to t('.stations'), admin_stations_path, class: "nav-link" %>
                        </li>
                        <li class="nav-item">
                            <%= link_to t('.routes'), admin_routes_path, class: "nav-link" %>
                        </li>
                        <li class="nav-item">
                            <%= link_to t('.trains'), admin_trains_path, class: "nav-link" %>
                        </li>
                        <li class="nav-item">
                            <%= link_to t('.wagons'), admin_wagons_path, class: "nav-link" %>
                        </li>
                        <li class="nav-item">
                            <%= link_to t('.tickets'), admin_tickets_path, class: "nav-link" %>
                        </li>
                    <% else %>
                        <li class="nav-item">
                            <%= link_to t('.my_tickets'), show_all_ticket_path(user_id: current_user.id), class: "nav-link" %>
                        </li>
                        <li class="nav-item">
                            <%= link_to t('.search_tickets'), find_ticket_path, class: "nav-link" %>
                        </li>
                    <% end %>
                    <li class="nav-item">
                        <%= button_to t('common.log_out'), destroy_user_session_path, method: :delete, class: "btn btn-outline-success" %>
                    </li>
                <% else %>
                    <li class="nav-item">
                        <%= link_to t('common.sign_in'), new_user_session_path, class: "btn btn-outline-success" %>
                    </li>
                <% end %>
            </ul>
        </div>
    </div>
</nav>