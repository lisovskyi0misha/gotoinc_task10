<%= bootstrap_form_with url: new_admin_train_wagon_path(train_id: params[:train_id]), method: :GET do |f| %>
    <%= f.select :wagon_type, @collection, label: t('.wagon_type') %>
    <%= f.submit t('.choose') %>
<% end %>
