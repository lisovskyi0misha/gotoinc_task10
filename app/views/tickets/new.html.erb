<%= bootstrap_form_for @user, url: ticket_path, method: :POST do |f| %>
    <%= f.text_field :name %>
    <%= f.text_field :passport_code %>
    <%= f.hidden_field :train_id, value: @train_id%>
    <%= f.submit %>
<% end %>