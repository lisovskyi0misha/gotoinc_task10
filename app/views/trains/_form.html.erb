<%= form_with(model: train) do |form| %>
  <% if train.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(train.errors.count, "error") %> prohibited this train from being saved:</h2>

      <ul>
        <% train.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :number, style: "display: block" %>
    <%= form.text_field :number %>
    <%= form.label :route_id, 'Route' %>
    <%= form.collection_select :route_id, Route.all, :id, :title%>
     <%= form.label :route_id, 'Current station' %>
    <%= form.collection_select :station_id, Station.all, :id, :title%>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
